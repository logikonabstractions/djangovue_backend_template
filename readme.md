# Django-vuejs project template

This project is half of a django-vuejs dev setup for these project types. It is generic (only provides simple model/views to demo).

# Django - structure
## General notes
* cors-headers managed by
* rest_framework used for routes
* .idea is gitted. This is intentional, so that I can easily re-import the run configurations etc. if ever running this on some other system. Just .gitignore it if you don't want that.
* 